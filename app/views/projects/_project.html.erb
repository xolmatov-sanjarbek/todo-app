<%= link_to project_path(project) do %>
  <div id="<%= dom_id project %>" class="w-full min-w-4xl sm:w-auto my-5 space-y-5 bg-amber-200 rounded-2xl p-5">
    <div>
      <strong class="block font-bold mb-1">Name:</strong>
      <%= project.name %>
    </div>
  </div>
<% end %>

